# =============================================================================
# PHP-FPM Automation Agent — Deployment Config
# =============================================================================
# Just provide the basics — the agent auto-detects everything else:
#   ✓ PHP version (from composer.json)
#   ✓ Document root (/public, /web, etc.)
#   ✓ PHP extensions needed
#   ✓ Database type (MySQL/PostgreSQL/SQLite) & auto-installs it
#   ✓ SQL schema files → auto-creates databases & imports tables
#   ✓ Composer install (latest from getcomposer.org)
#   ✓ Framework-specific commands (Laravel artisan, etc.)
# =============================================================================

service_name: todo-php
domain: 98.81.165.46
repo_url: https://github.com/AbdurRazzaq2004/sample-php-app-for-automationTest.git
branch: main
deploy_path: /var/www/todo-php
web_server: apache
